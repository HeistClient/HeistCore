package ht.heist.core.ui;

/**
 * Color palette choices for the heatmap overlay and export.
 * Make sure this file name EXACTLY matches the public class name.
 */
public enum HeatmapColorMode {
    MONO,
    HOT_COLD,
    INFRARED
}
